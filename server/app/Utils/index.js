const slugify = require('./slugify')
const capitalize = require('./capitalize')
const castStringAsArray = require('./castStringAsArray')

module.exports = {
  slugify,
  capitalize,
  castStringAsArray,
}
